build:
  contacts-service:
    context: .

deploy:
  - helm upgrade --install contacts-service chart
    --set contacts.image=${OKTETO_BUILD_CONACTS_SERVICE_IMAGE}

dev:
  contacts-service:
    command: nmp start
    sync:
      - .:/app